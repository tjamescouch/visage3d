# web

Browser-based 3D viewer surface.

## capabilities

- render a 3D avatar model in the browser
- control camera and lighting defaults
- play one or more included animations

## interfaces

exposes:
- a reusable viewer component (importable module)
- a set of isolated visual harness pages for each component

depends on:
- assets component for providing a local URL/path to the model
